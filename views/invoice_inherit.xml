<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <!-- 
        INVOICE TEMPLATE INHERITANCE FOR SPACEY LAYOUT
        
        This file inherits the standard Odoo invoice template to customize
        the formatting and positioning of invoice-specific elements when
        using the Spacey Layout.
        
        Original template: account.report_invoice_document
        Location: /usr/lib/python3/dist-packages/odoo/addons/account/views/report_invoice.xml
        -->
        
        <template id="report_invoice_document_spacey" inherit_id="account.report_invoice_document">
            
            <!-- CHANGE LAYOUT TO SPACEY -->
            <!-- Replace web.external_layout with our Spacey Layout -->
            <xpath expr="//t[@t-call='web.external_layout']" position="attributes">
                <attribute name="t-call">dh_efficient_layout.external_layout_spacey</attribute>
            </xpath>
            
            <!-- REMOVE ORIGINAL INVOICE TITLE -->
            <xpath expr="//h2" position="replace">
            </xpath>
            
            <xpath expr="//t[@t-set='address']" position="replace">
            </xpath>
            
            <!-- STYLE TABLE HEADERS WITH BOLD LINES -->
            <!-- Add bold borders to table headers like Subtotal/Total -->
            <xpath expr="//table[@name='invoice_line_table']//thead//tr" position="attributes">
                <attribute name="style">border-top: 2px solid #000; border-bottom: 2px solid #000;</attribute>
            </xpath>
            
            <!-- STYLE TABLE HEADER CELLS -->
            <!-- Make header text bold and add padding -->
            <xpath expr="//table[@name='invoice_line_table']//thead//th" position="attributes">
                <attribute name="style">font-weight: bold; padding: 8px 8px; background-color: #f8f9fa;</attribute>
            </xpath>
            
            <!-- CUSTOMIZE INVOICE INFORMATIONS SECTION -->
            <!-- Replace with spacey 4-column form-style layout for maximum efficiency -->
            <xpath expr="//div[@id='informations']" position="replace">
                <!-- <div id="informations" class="mt16 mb16" style="border-top: 1px solid #000; border-bottom: 1px solid #000; padding: 12px 0;"> -->
                <div id="informations" class="mt16 mb16" style="border-top: 1px solid #000; padding: 12px 0;">
                    
                    <!-- SET INVOICE ADDRESS VARIABLES ONCE -->
                    <t t-set="invoice_address_id" t-value="o.partner_id.address_get(['invoice'])['invoice'] if o.partner_id else False"/>
                    <t t-set="invoice_partner" t-value="o.env['res.partner'].browse(invoice_address_id) if invoice_address_id else o.partner_id"/>
                    
                    <!-- Simplified: Just use the original partner for customer name (it's always correct) -->
                    <t t-set="display_partner" t-value="o.partner_id"/>
                    <t t-set="address_partner" t-value="invoice_partner"/>                    <!-- FIRST ROW: Invoice Date | Value | Due Date | Value -->
                    <div class="row mb-2" t-if="o.invoice_date or (o.invoice_date_due and o.type == 'out_invoice' and o.state == 'posted')">
                        <!-- Invoice Date Label -->
                        <div class="col-3" t-if="o.invoice_date">
                            <strong style="color: #000; font-size: 15px;">
                                <t t-if="o.type == 'out_invoice'">Invoice Date:</t>
                                <t t-elif="o.type == 'out_refund'">Credit Note Date:</t>
                                <t t-else="">Date:</t>
                            </strong>
                        </div>
                        <!-- Invoice Date Value -->
                        <div class="col-3" t-if="o.invoice_date">
                            <span style="font-size: 15px;" t-field="o.invoice_date"/>
                        </div>
                        
                        <!-- Due Date Label -->
                        <div class="col-3" t-if="o.invoice_date_due and o.type == 'out_invoice' and o.state == 'posted'">
                            <strong style="color: #000; font-size: 15px;">Due Date:</strong>
                        </div>
                        <!-- Due Date Value -->
                        <div class="col-3" t-if="o.invoice_date_due and o.type == 'out_invoice' and o.state == 'posted'">
                            <span style="font-size: 15px;" t-field="o.invoice_date_due"/>
                        </div>
                        
                        <!-- Fill remaining columns if only one field exists -->
                        <div class="col-6" t-if="o.invoice_date and not (o.invoice_date_due and o.type == 'out_invoice' and o.state == 'posted')"></div>
                        <div class="col-6" t-if="not o.invoice_date and (o.invoice_date_due and o.type == 'out_invoice' and o.state == 'posted')"></div>
                    </div>
                    
                    <!-- SECOND ROW: Source | Value | (no customer already in header) -->
                    <div class="row mb-2" t-if="o.invoice_origin">
                        <!-- Source Label -->
                        <div class="col-3" t-if="o.invoice_origin">
                            <strong style="color: #000; font-size: 15px;">Source:</strong>
                        </div>
                        <!-- Source Value -->
                        <div class="col-3" t-if="o.invoice_origin">
                            <span style="font-size: 15px;" t-field="o.invoice_origin"/>
                        </div>
                        
                        <!-- Empty columns (customer info is already in header area) -->
                        <div class="col-6">
                        </div>
                    </div>
                    
                    <!-- THIRD ROW: VAT | Value | Reference | Value -->
                    <div class="row mb-2" t-if="o.ref or (o.partner_id and invoice_partner.vat)">
                        <!-- VAT Label (using invoice address partner) -->
                        <div class="col-3" t-if="o.partner_id and invoice_partner.vat">
                            <strong style="color: #000; font-size: 15px;">
                                <t t-if="o.company_id.country_id.vat_label" t-esc="o.company_id.country_id.vat_label"/>
                                <t t-else="">Tax ID</t>:
                            </strong>
                        </div>
                        <!-- VAT Value -->
                        <div class="col-3" t-if="o.partner_id and invoice_partner.vat">
                            <span style="font-size: 15px;" t-field="invoice_partner.vat"/>
                        </div>
                        
                        <!-- Reference Label -->
                        <div class="col-3" t-if="o.ref">
                            <strong style="color: #000; font-size: 15px;">Reference:</strong>
                        </div>
                        <!-- Reference Value -->
                        <div class="col-3" t-if="o.ref">
                            <span style="font-size: 15px;" t-field="o.ref"/>
                        </div>
                        
                        <!-- Fill remaining columns if only one field exists -->
                        <div class="col-6" t-if="(o.partner_id and invoice_partner.vat) and not o.ref"></div>
                        <div class="col-6" t-if="not (o.partner_id and invoice_partner.vat) and o.ref"></div>
                    </div>
                    
                    <!-- FOURTH ROW: Customer Address (spans full width for long addresses) -->
                    <div class="row" t-if="o.partner_id">
                        <div class="col-3">
                            <strong style="color: #000; font-size: 15px;">Address:</strong>
                        </div>
                        <div class="col-9">
                            <span style="font-size: 15px;">
                                <t t-if="invoice_partner.street"><span t-field="invoice_partner.street"/></t><t t-if="invoice_partner.street2">, <span t-field="invoice_partner.street2"/></t><t t-if="invoice_partner.city">, <span t-field="invoice_partner.city"/></t><t t-if="invoice_partner.state_id"> <span t-field="invoice_partner.state_id.name"/></t><t t-if="invoice_partner.zip"> <span t-field="invoice_partner.zip"/></t><t t-if="invoice_partner.country_id">, <span t-field="invoice_partner.country_id.name"/></t>
                            </span>
                        </div>
                    </div>
                    
                </div>
            </xpath>
            
        </template>
        
    </data>
</odoo>